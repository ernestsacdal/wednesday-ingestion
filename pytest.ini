[pytest]
# Tests cover PURE logic only (derivation, week math, predictor, recipe
# slotting) — no DB, no network, no secrets — so the suite runs anywhere.
pythonpath = .
testpaths = tests
python_files = test_*.py
addopts = -q
